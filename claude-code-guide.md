# Claude Code 進め方ガイド(フェーズ別プロンプト付き)

自分用の手順書。上から順にやれば OK。

## 0. 事前準備(1回だけ)

1. **Git をインストール**(Windows なら https://git-scm.com/ から Git for Windows)。ターミナルで `git --version` と打って番号が出れば OK
2. プロジェクト用フォルダを作る(例: `piano-fingering-app`)
3. その中に以下の 2 ファイルを置く
   - `piano-fingering-app-spec.md`(仕様書)
   - `CLAUDE.md`(開発ルール。Claude Code が毎回自動で読む)
4. ターミナルでそのフォルダに移動して `claude` で起動
5. 最初にこれを送る(初回コミットまで済ませる):

```
git init して、CLAUDE.md と piano-fingering-app-spec.md を最初のコミットとして保存してください。
あわせて空の LESSONS.md と docs/plans/ フォルダも作ってコミットしてください。
```

## 1. 基本の進め方: 「プランモード → 承認 → 実装」

先生の言う「編集を承認で」はプランモードのこと。

- Claude Code 起動後、**Shift+Tab を押してモードを切り替え**、画面下部の表示が plan mode になったことを確認する
- プランモード中の Claude はファイルを読んで計画を作るだけで、**承認するまでコードを一切変更しない**
- 出てきた計画を読んで、おかしい所があれば日本語で指摘 → 計画を修正させる → 納得したら承認して実装させる

### 毎フェーズの流れ(教授ルール対応版)

1. Shift+Tab でプランモードにする
2. 下のフェーズ別プロンプトを貼る → 計画が提示される
3. 計画に納得したら、実装を承認する**前に**こう送る:

```
その計画を docs/plans/P◯-plan.md に保存してください。
そのあとサブエージェントを使って、この計画を仕様書(piano-fingering-app-spec.md)と
照らし合わせてレビューさせてください。観点: 仕様との整合、抜け漏れ、仕様にない余計な実装。
指摘があれば計画を修正して、修正後の計画を見せてください。
```

4. サブエージェントのレビューが OK になったら実装を承認
5. 実装が終わったら動作確認手順に従って自分で動かして確認
6. OK なら commit(CLAUDE.md のルールで作業ごとに commit するよう指示済みだが、フェーズ完了時は「ここまでを commit して」と念押しすると確実)
7. 途中でハマった問題・やってはいけないと分かったことがあれば「LESSONS.md に記録して」と指示してから次へ

## 2. フェーズ別プロンプト(コピペ用)

### P1: 雛形 + バーチャル MIDI + 判定エンジン

```
piano-fingering-app-spec.md を読んでください。
フェーズP1(11章)を実装します。対象:
- Vite + TypeScript + React のプロジェクト雛形(仕様書5章・13章のディレクトリ構成)
- バーチャルMIDIキーボード(F-02)
- 判定エンジンのうちスコアフォロー(7.1)と音判定(7.3の音判定部分)
- テスト曲「ドレミファソファミレド」のJSONデータ(8.3)
- src/core/ のユニットテスト
運指判定とカメラ関連はまだ実装しないでください。
まず実装計画を提示してください。
```

### P2: カメラ + MediaPipe

```
フェーズP2を実装します。仕様書の該当箇所: 7.2の入力部分、F-01のカメラ選択。
- getUserMedia でのカメラ選択・プレビュー
- MediaPipe Hands の組み込み(両手・21点ランドマーク)
- プレビュー上へのランドマーク描画
- ランドマーク履歴のリングバッファ(7.2の重要注記)
判定との接続はまだ不要です。まず計画を出してください。
```

### P3: キャリブレーション + 指特定

```
フェーズP3を実装します。仕様書 F-03 と 7.2 に従ってください。
- キャリブレーションウィザード(F-03の手順1〜5)
- 指特定ロジック(7.2の推定ロジック1〜5。信頼度と判定不能の扱いを含む)
- デバッグ表示: Note Onごとに「推定した指番号と信頼度」を画面に出す
バーチャルMIDIでも動作確認できるようにしてください。まず計画を出してください。
```

### P4: 運指判定の統合 + フィードバック

```
フェーズP4を実装します。
- 運指判定(7.3)を判定エンジンに統合
- Web Audio によるビープ(F-05: 運指ミスと音ミスで音を変える)
- 練習画面(S-04)の完成: 進行位置表示、一時停止、やり直し、判定ログ記録
まず計画を出してください。
```

### P5: 結果画面 + 設定 + 楽曲追加

```
フェーズP5を実装します。
- 結果サマリー画面(F-06 / S-05)
- 設定(F-07 / S-06)
- MusicXML→JSON変換スクリプト(8.2、tools/に配置)
変換スクリプトの使い方をREADMEに書いてください。まず計画を出してください。
```

### P6: Android 対応 + デプロイ

```
フェーズP6を実装します。
- PWA化(vite-plugin-pwa)
- スマホ縦持ちレイアウトの調整(9章)
- GitHub Pages へのHTTPSデプロイ設定(10章: カメラとMIDIはHTTPS必須)
- Android Chrome での確認手順書の作成
まず計画を出してください。
```

## 3. 実機が届いたときにやること

### MIDI キーボード(microKEY2 Air-61)が届いたら

- まず **USB ケーブルで PC に接続**して動作確認(Windows の Bluetooth MIDI は不安定なことがあるため)
- セットアップ画面のデバイス一覧に表示されるか確認
- Claude Code への指示例: 「実MIDIキーボードを接続しました。バーチャルMIDIと切り替えて動くか確認したいので、確認手順を教えてください」

### Android 実機が用意できたら

- OS の Bluetooth 設定でキーボードとペアリング
- デプロイ済み URL を Android Chrome で開き、カメラ・MIDI の権限を許可
- P6 の確認手順書に従って全機能チェック

## 4. 困ったときのコツ

- 実装が仕様とズレてきたと感じたら: 「仕様書の◯章と今の実装が合っているか確認して」と指示する
- 変更が大きくなりそうなときは必ずプランモードに戻す(Shift+Tab)
- エラーが出たら、エラーメッセージを丸ごと貼って「日本語で原因を説明してから直して」と頼む
- 1 フェーズ終わるごとに git commit。壊れたら戻れるようにする
