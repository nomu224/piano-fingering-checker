# CLAUDE.md — ピアノ運指チェッカー開発ルール

このファイルは Claude Code への恒常的な指示です。すべての作業で従ってください。

## プロジェクト概要

ピアノ初心者向けの運指(指遣い)チェック Web アプリ。カメラ(MediaPipe Hands)で指を認識し、MIDI キーボード入力と照合して、譜面と違う指で弾いたらビープ音で知らせる。卒業制作。

## 最重要ルール

1. **仕様書 `piano-fingering-app-spec.md` が唯一の正**。実装で迷ったら必ず仕様書を参照すること
2. 仕様書と矛盾する実装・仕様にない機能追加を勝手にしない。仕様変更が必要だと判断したら、実装せずにまず提案して承認を得る
3. フェーズ(仕様書 11 章)を飛ばさない。指示されたフェーズの範囲だけ実装する
4. 開発者は学生で、AI 支援を前提に開発している。**変更内容は日本語で分かりやすく説明**すること

## 技術スタック(仕様書 5 章の要約)

- Vite + TypeScript + React
- MediaPipe Hands (`@mediapipe/tasks-vision`)
- Web MIDI API(ラッパーライブラリ禁止)
- Web Audio API(ビープ音)
- テスト: Vitest

## コーディング規約

- TypeScript strict モード
- `src/core/`(判定エンジン)は **UI・ブラウザ API に依存しない純 TypeScript** にする。ここは必ずユニットテストを書く
- コメントは日本語で書く(卒論・発表で本人がコードを説明できるようにするため)
- 外部ライブラリの追加は最小限。追加する前に理由を説明して承認を得る
- ディレクトリ構成は仕様書 13 章に従う

## 動作確認

- `npm run dev` で起動し、動作確認手順を毎回提示すること
- `npm run test` が通らない状態でフェーズ完了としない

## 環境上の注意

- 開発者の手元にはまだ MIDI キーボード実機・Android 実機がない
- そのため**バーチャル MIDI(仕様書 F-02)を常に動く状態に保つ**こと。実デバイス前提のコードで開発を止めない
- カメラは PC の Web カメラまたはスマホの Web カメラ化アプリを使用

## Git 運用(絶対ルール)

- 作業開始時に `git status` でリポジトリ状態を確認する。リポジトリでなければ `git init` する
- **1 つの作業単位(機能実装・バグ修正・ファイル追加)が終わるたびに、必ず commit する。例外なし**
- commit メッセージは日本語で「何をしたか」を簡潔に書く
- 大きな変更・リファクタリングの前にも commit してから着手する(壊れたら戻せるように)

## 計画ファースト(実装前の必須手順)

1. 実装に入る前に、これからやることを `docs/plans/` 配下の md に書き出す(例: `docs/plans/P1-plan.md`)。内容: 目的 / 対象ファイル / 手順 / 完了条件 / 仕様書の該当章
2. 計画 md を**サブエージェントに渡し、仕様書と照らしてレビューさせる**(観点: 仕様との整合、抜け漏れ、やり過ぎ)
3. レビューで OK が出てから実装に着手する。指摘があれば計画を修正して再レビュー
4. 実装は計画 md と仕様書に正確に従う。**仕様書は神。逸脱・独自判断での追加は禁止**

## 失敗の記録(LESSONS.md)

- プロジェクト直下に `LESSONS.md` を置き、発生した問題・原因・再発防止策を記録する
- 「やってはいけないこと」が判明したら即座に追記する
- **毎セッション開始時に LESSONS.md を読み、記録された失敗を二度と繰り返さないこと**

## 進捗管理

- 各フェーズ完了時に、このファイル末尾の「進捗ログ」に完了日と内容を 1 行追記すること

## 進捗ログ

- 2026-07-05: P1 完了(Vite+React+TS 雛形 / バーチャル MIDI F-02 / スコアフォロー 7.1・音判定 / テスト曲 JSON / ユニットテスト 19 件)
- 2026-07-06: P2 完了(カメラ選択・プレビュー F-01 / MediaPipe Hands 両手 21 点 / ランドマーク描画 / 履歴リングバッファ 7.2)
- 2026-07-06: handedness の実カメラ検証 OK(左手=水色/右手=オレンジで一致。読み替え不要、SWAP_HANDEDNESS=false で確定)
- 2026-07-06: P3 完了(キャリブレーションウィザード F-03 / 指特定ロジック 7.2 / 指特定デバッグ表示。ユニットテスト計 45 件)
- 2026-07-07: 鍵盤補間を実鍵盤ジオメトリに仕様変更(開発者承認)。P3 の実カメラ通し確認 OK(ウィザード→指特定まで動作)
- 2026-07-07: P4 完了(運指判定統合 7.3 / ビープ F-05 / 練習画面 S-04: 一時停止・やり直し・小節指定開始・判定ログ・カメラなしモード。ユニットテスト計 67 件。実カメラでの運指ミスビープ確認は開発者に依頼中)
