# ピアノ運指チェッカー 仕様書

- 作成日: 2026-07-03
- 種別: 卒業制作(個人開発)
- 本書の用途: Claude Code への開発指示のベースとなる仕様書

---

## 1. 概要

ピアノ初心者向けの運指(指遣い)練習支援 Web アプリ。

カメラで演奏者の手を撮影して指を認識し、MIDI キーボードからの入力(どの鍵盤をいつ押したか)と突き合わせることで、「譜面で指定された指番号と違う指で弾いた」ことをリアルタイムに検知して知らせる。

**機能の優先度: 本アプリの主目的は運指(指遣い)の正誤判定である。音の正誤判定は、運指判定に必要な「譜面上の現在位置の特定」の副産物として提供する従属機能であり、実装・評価のリソース配分は常に運指判定を優先する。**

### 1.1 解決したい課題

- 独学の初心者は運指の間違いに自分で気づけず、悪い癖のまま定着してしまう
- 既存の練習アプリは「音の正誤」は判定できるが「どの指で弾いたか」までは見ていない

### 1.2 最終ゴール

**Android スマホ 1 台 + MIDI キーボードだけで完結する**こと。

- スマホを鍵盤の上方にスタンドで固定し、カメラで手を撮影
- MIDI キーボードは Bluetooth MIDI (BLE-MIDI) でスマホにワイヤレス接続(USB ケーブル接続も可能なため、Bluetooth が不安定な場合の代替手段とする)
- フィードバックは音(ビープ)を主とし、画面を見なくても練習できる

### 1.3 開発時の環境

現時点で Android 実機・MIDI キーボードが未入手のため、**PC のブラウザで開発**する。Web アプリ(PWA)として作ることで、PC で動いたものがそのまま Android Chrome で動く構成とする。

- 開発中のカメラ: PC の Web カメラ、またはスマホの Web カメラ化アプリ
- 開発中の MIDI: キーボード入手までは**バーチャル MIDI キーボード(画面上のクリック/PC キーボード操作で Note On/Off を発生させるデバッグ機能)**で代替する。この機能は必ず初期に実装すること

---

## 2. スコープ

### 2.1 やること

| # | 内容 |
|---|------|
| 1 | カメラ映像から両手の指先位置をリアルタイム検出 |
| 2 | Web MIDI API による鍵盤入力(Note On/Off)の取得 |
| 3 | 鍵盤とカメラ映像の対応付け(キャリブレーション) |
| 4 | 打鍵ごとの「使用した指」の推定 |
| 5 | 譜面データとの照合による運指の正誤判定(メイン機能) |
| 6 | 音の正誤判定(押した音が譜面と合っているか) |
| 7 | 間違い時の音フィードバック(ビープ)+ 画面表示 |
| 8 | 練習結果のサマリー表示(どの音符で間違えたか) |
| 9 | 楽曲データの内蔵(バイエルの練習曲 数曲から開始) |

### 2.2 やらないこと(明示的にスコープ外)

- リズム・タイミングの評価(弾く速さは自由。スコアフォロー方式で追従する)
- 音の強弱・表現の評価
- 楽譜の表示・追従表示(将来拡張とする。まずは判定に集中)
- ユーザーが任意の MIDI ファイルをアップロードして使う機能(運指データが付けられないため。将来拡張)
- iOS 対応(Web MIDI API が iOS Safari で使えないため)
- ペダル、和音の高度な解析(和音は「同時押し」として個別音の集合で扱う)

---

## 3. 動作環境

| 項目 | 開発時 | 最終形 |
|------|--------|--------|
| 端末 | PC (Windows/Mac) | Android スマホ 1 台 |
| ブラウザ | Chrome 最新 | Android Chrome 最新 |
| カメラ | PC Web カメラ | スマホ背面カメラ(上方スタンド固定) |
| MIDI 入力 | バーチャル MIDI(デバッグ機能) → KORG microKEY2 Air-61(PC では USB 接続推奨) | 同キーボードを Bluetooth MIDI 接続(代替: USB OTG) |
| 配信 | localhost (Vite dev server) | HTTPS 必須(GitHub Pages 等)。カメラ・MIDI とも HTTPS でないと動かない |

### 3.1 対象キーボード

KORG microKEY2 Air-61(61 鍵、Bluetooth MIDI + USB MIDI 両対応)。ただし**特定機種に依存する実装はしない**こと。Web MIDI API で見える任意の MIDI 入力デバイスを選択できる UI にする。

接続方法の注意:

- **Bluetooth のペアリングは OS 側で行う**(Android の設定、または KORG BLE-MIDI ドライバ等)。ペアリング済みであれば Web MIDI API からは USB と同じ「MIDI 入力デバイス」として見えるため、**アプリ側に Bluetooth 固有の実装は不要**
- Android: OS 標準で BLE-MIDI に対応。ペアリング後 Chrome の Web MIDI に現れる
- Windows での開発時: Windows は BLE-MIDI のブラウザ連携が不安定なことがあるため、**PC 開発中は USB ケーブル接続を推奨**(Air は USB でも使える)
- セットアップ画面(F-01)に「キーボードが一覧に出ない場合は OS の Bluetooth 設定でペアリングを確認」というガイド文を表示すること

---

## 4. システム構成

すべてブラウザ内で完結する(サーバーサイド処理なし)。

```
┌─────────────────────────────────────────────┐
│ ブラウザ (Chrome / Android Chrome)            │
│                                               │
│  カメラ映像 ──→ MediaPipe Hands (WASM)        │
│  (getUserMedia)   └─ 手21点ランドマーク       │
│                          │                    │
│  MIDIキーボード ──→ Web MIDI API              │
│  (Note On/Off)        │                       │
│                       ▼                       │
│              ┌──────────────┐                 │
│              │ 判定エンジン   │←─ 楽曲データ   │
│              │ ・スコアフォロー│   (JSON内蔵)  │
│              │ ・指の特定     │                │
│              │ ・運指/音判定  │                │
│              └──────┬───────┘                 │
│                     ▼                         │
│   フィードバック (Web Audio ビープ/参照音 + UI) │
└─────────────────────────────────────────────┘
```

---

## 5. 技術スタック(Claude Code への指定)

| 領域 | 技術 | 備考 |
|------|------|------|
| ビルド | Vite + TypeScript | |
| UI | React | 状態管理は useState/useReducer で十分。外部状態管理ライブラリ不要 |
| 手認識 | MediaPipe Hands (`@mediapipe/tasks-vision` の HandLandmarker) | numHands: 2、GPU delegate 優先 |
| MIDI | Web MIDI API(標準 API 直接使用) | ラッパーライブラリ不要 |
| 音 | Web Audio API | ビープ音と参照音の生成のみ(音源ファイルは読み込まない) |
| PWA | vite-plugin-pwa | オフライン動作は必須ではない。ホーム画面追加ができれば十分 |
| テスト | Vitest | 判定エンジンなど純ロジック部分のみユニットテスト対象 |

**方針: 判定エンジン(スコアフォロー・指特定・正誤判定)は UI や API から独立した純 TypeScript モジュールとして実装し、テスト可能にすること。**

---

## 6. 機能要件

### F-01 デバイスセットアップ画面
- カメラの選択・プレビュー表示
- **映像の左右反転(ミラー)切り替え**を設ける。デフォルトは反転なし(=カメラが見たままの映像)とし、内部処理(ランドマーク座標・handedness)は常にこの生映像基準で統一する。プレビュー表示だけを反転する実装にし、座標系には反転を持ち込まないこと
- **「カメラなしで開始(音判定のみモード)」ボタン**を設ける。この場合キャリブレーション(S-02)をスキップして楽曲選択へ進み、練習中は運指判定を行わず音判定のみとする(P1 の動作確認およびカメラ環境がない場合の利用のため)
- MIDI 入力デバイスの一覧表示・選択(未接続時は「バーチャル MIDI」を選択可能)
- 権限リクエストの誘導(カメラ / MIDI とも)

### F-02 バーチャル MIDI(デバッグ機能)
- 画面上に 1〜2 オクターブ分の鍵盤 UI を表示し、クリック/タップで Note On/Off を発生させる
- **オクターブシフトボタン(±)を付ける**(バイエル両手曲は左手が低音域のため、シフトがないとテストできない)
- PC キーボードのキー(例: A=ド, W=ド♯, S=レ...)でも入力可能(具体的なキー配置は実装時に決めて README に記載)
- 実 MIDI デバイスと同じインターフェースで判定エンジンに渡す(判定エンジン側は入力源を区別しない)

### F-03 キャリブレーション
目的: 「MIDI ノート番号 ⇔ カメラ映像内の鍵盤の x 座標」の対応表を作る。

手順(ガイド付きウィザード形式):
1. カメラ映像を表示し、鍵盤全体が映るようセッティングさせる
2. 「低い方の基準鍵盤を 1 つ押してください」→ Note On と同時に、そのとき最も下(鍵盤側)にある指先ランドマークの x 座標を記録。**どの鍵盤かはユーザーの任意で良い**(ノート番号は MIDI から取得できるため)。ただし手順 3 の鍵盤と 1 オクターブ以上離すようガイドに明記(近いと補間精度が落ちる)
3. 「高い方の基準鍵盤を 1 つ押してください」→ 同様に記録
4. 2 点間から全鍵盤の x 座標を算出する。**白鍵は等幅、黒鍵は隣り合う白鍵の境目にある**という実鍵盤の配置(ジオメトリ)に基づく線形補間とする(鍵盤サイズには非依存)
5. 確認画面: 適当な鍵盤を数回押させ、推定された鍵盤位置とのズレを表示。**ズレが 0.5 半音間隔(=おおよそ隣の鍵盤と取り違えるレベル)を超えた打鍵が 1 回でもあれば再キャリブレーションを促す**(強制はしない。閾値はパラメータ化)
- キャリブレーション結果は localStorage ではなく**メモリ内保持のみ**(カメラ位置が毎回変わるため、セッションごとに実施)
- カメラの選択・反転設定を変更した場合は再キャリブレーションを必須とする(座標系が変わるため)

### F-04 楽曲選択
- 内蔵楽曲(バイエルの練習曲など)の一覧から選択
- 曲名・難易度・小節数を表示

### F-05 練習モード(メイン画面)
- カメラプレビュー + 検出中の手ランドマークのオーバーレイ表示
- 現在の進行位置(次に弾くべき音と指番号)の表示
- 打鍵ごとにリアルタイム判定:
  - **運指ミス**: 譜面と違う指で弾いた → ビープ音 A(低め・ブザー的)+ 画面に「指番号: 正解 3 / 実際 2」のような表示
  - **音ミス**: 譜面と違う鍵盤を弾いた → ビープ音 B(音ミスと運指ミスは音色を変える)
  - **正解**: 無音(または設定でクリック音)
- 一時停止 / 最初からやり直し / 曲の途中の任意位置から開始(**小節番号で指定**し、内部でその小節の最初のイベント index に変換する)
- 判定ログを記録(タイムスタンプ、譜面上の位置、期待値、実測値、判定結果)

### F-06 結果サマリー
- 曲(または中断)終了後に表示
- 総打鍵数、運指ミス数、音ミス数、指特定に失敗した数(判定不能数)
- 譜面位置ごとのミス一覧(「◯小節目の◯番目の音: 3 の指のところを 2 で弾いた」)
- **判定ログの JSON ダウンロード機能**(卒業論文の精度評価実験でデータとして使うため)

### F-07 設定
- フィードバック音の ON/OFF・音量
- 判定の厳しさ(後述の指特定で信頼度が低いときに「判定不能として無視する」か「ミス扱いにする」か)
- 左手/右手/両手の判定対象切り替え

### F-08 参照音の再生

MIDI キーボードは音源を持たない機種が多く、音が鳴らないままでは練習にならないため、
アプリ側で押された音を鳴らす。

- **Note On 受信時、押された音の高さに対応する参照音を Web Audio API で即座に再生する**
- 音色はオシレーター(倍音を数本重ねる)+ ADSR エンベロープ(速いアタック、緩やかな減衰)で構成し、
  ピアノらしい響きに寄せる。**音源ファイルの読み込みは行わない**(遅延を最小化するため)
- **AudioContext はアプリ起動時に 1 度だけ生成**し、Note On のたびに再生成しない
- **発音処理は判定処理より先に呼び出し**、体感遅延を最小化する
- 参照音は「押された音」をそのまま鳴らす(譜面と違う音でも鳴る。実際のピアノと同じ挙動)

---

## 7. コアアルゴリズム

### 7.1 スコアフォロー(譜面追従)

テンポは評価しないため、**MIDI 入力主導で譜面上のカーソルを進める**方式とする。

- 譜面データは時系列の「イベント列」(単音またはコード=同時発音の集合)として持つ
- Note On が来たら現在カーソルのイベントと照合:
  - 期待音と一致 → 正解処理してカーソルを進める(コードの場合は構成音がすべて揃ったら進める)
  - 不一致 → 音ミスとして記録。カーソルは進めない(弾き直しを待つ)
- **和音(複数 notes のイベント)の照合ルール**:
  - イベント内の未達成の構成音が来たら達成マークを付ける。全構成音が達成された時点でカーソルを進める(同時押しである必要はなく、順に押しても良い)
  - 構成音以外の音が来たら音ミスとして記録(達成済みマークはリセットしない)
  - 達成済みの構成音と同じ音がもう一度来た場合は無視(ミスにしない)
- **同音連打**: 連続するイベントが同じ音でも、イベントごとに新しい Note On が必要(押しっぱなしでは進まない)。ユニットテストに同音連打のケースを必ず含めること
- **曲の終了**: 最終イベントの達成をもって演奏終了とし、自動で結果画面(S-05)へ遷移する
- 連続ミスでカーソルが詰まった場合の救済: **同一カーソル位置で 2 回連続して音ミスが発生した時点で**、直後 N イベント(N=2、パラメータ化)以内に直近のミス音と一致する音があるか探索する。あれば「弾き飛ばし」とみなしてカーソルをジャンプし、飛ばされたイベントは「スキップ(未演奏)」として判定ログに記録する。この救済は単音イベントのみ対象とし、和音イベントには適用しない

### 7.2 指の特定(このプロジェクトの核心)

Note On 受信時に、直近のカメラフレームの手ランドマークから「どの指で押したか」を推定する。

**処理単位の原則: 判定はイベント単位ではなく Note On 1 回ごとに独立して行う。** Note On が来たら、①スコアフォローが現在イベント内の未達成 notes から一致するノート番号の note を特定し、②その note の `hand` を使って以下の指特定を行い、③その note 単位で運指判定・記録する。イベントは達成状態の管理(全 notes 達成でカーソル前進)だけを担う。これにより両手同時の和音でも、右手音と左手音は別々の Note On として自然に個別判定される。

入力:
- MediaPipe Hands の 21 点ランドマーク × 最大 2 手(handedness: Left/Right 付き)
- キャリブレーション結果(ノート番号 → 画像 x 座標)
- Note On のノート番号

推定ロジック(初期実装):
1. Note On のノート番号から鍵盤の x 座標 `kx` を得る
2. **距離計算の対象は、譜面イベントの `hand` に一致する側の手のみ**とする(反対の手は候補から除外。両手が近接したときの誤判定を防ぐ)。対象の手が検出されていない場合は「判定不能」を返す
3. 対象の手の 5 指の指先ランドマーク(landmark 4, 8, 12, 16, 20)について `|指先x - kx|` を計算
4. 最小距離の指先を「押した指」と推定。指番号への変換: 親指=1, 人差し指=2, 中指=3, 薬指=4, 小指=5
5. 信頼度スコア(具体値。単位はすべて「平均半音間隔」= キャリブレーションで得たオクターブ幅の 1/12 を 1.0 とする比率。解像度に依存させないため):
   - 最小距離が **0.6 半音間隔** を超える場合 → どの指も鍵盤に十分近くない → 「判定不能」
   - 1 位と 2 位の距離差が **0.25 半音間隔** 未満 → 拮抗とみなし、y 座標(押下時は指先が下がる=鍵盤に近い)を第 2 の判定材料に使う。それでも y 座標差が僅差(画像高さの 2% 未満)なら「判定不能」
   - **これらの閾値はすべて仮の初期値であり、実機での予備実験で調整する前提**。`src/core/` 内の定数ファイル 1 箇所に集約し、コメントで「実験で調整するパラメータ」と明記すること

**handedness の注意**: MediaPipe の handedness ラベルは映像の反転状態に依存する(セルフィービュー前提のラベル付けのため、非反転映像では Left/Right を読み替える必要がある可能性がある)。実装時に実映像で必ず検証し、読み替えの有無を 1 箇所の定数で切り替えられるようにしておくこと。

精度向上の拡張余地(初期実装では不要、コメントで TODO 化):
- Note On 直前数フレームの指先 y 座標の速度(下降検出)
- カメラの遅延と MIDI のタイムスタンプの補正

**重要: フレーム取得と Note On の時刻ズレ対策として、直近 5 フレーム程度のランドマーク履歴をリングバッファで保持し、Note On のタイムスタンプに最も近いフレームを使うこと。**

### 7.3 判定

- 音判定: スコアフォローの照合結果そのまま
- 運指判定: 音が正解だったイベントについて、推定指番号と譜面の指番号を比較
  - 一致 → OK
  - 不一致 → 運指ミス
  - 判定不能 → 設定に従い「無視(カウントのみ)」または「ミス扱い」
- 音ミスのイベントは運指判定の対象外(音を直すのが先のため)

---

## 8. 楽曲データ形式

MIDI ファイルは運指情報を持てないため、独自 JSON を内蔵する。元データはバイエルの運指付き MusicXML(MuseScore 等で入手)から変換する。

### 8.1 JSON スキーマ(案)

```json
{
  "id": "beyer-08",
  "title": "バイエル 8番",
  "difficulty": 1,
  "events": [
    {
      "index": 0,
      "measure": 1,
      "posInMeasure": 1,
      "notes": [
        { "midi": 60, "finger": 1, "hand": "R" }
      ]
    },
    {
      "index": 1,
      "measure": 1,
      "posInMeasure": 2,
      "notes": [
        { "midi": 62, "finger": 2, "hand": "R" },
        { "midi": 48, "finger": 5, "hand": "L" }
      ]
    }
  ]
}
```

- `events` は演奏順。`notes` が複数あるものは同時発音(両手含む)
- `posInMeasure`: 小節内の通し番号(1 始まり)。結果表示「◯小節目の◯番目の音」に使う。**タイミング情報は不要で、変換スクリプトがイベント順から自動採番する**
- `finger`: 1〜5。運指未指定の音符は `finger: null` とし、運指判定をスキップ
- 同一イベント内に同じノート番号が複数の手に存在するケース(左右の手で同じ鍵盤を同時に弾く)は初期収録曲では扱わない。変換スクリプトで検出したら警告を出すこと
- タイミング情報(拍・音価)は持たない(スコアフォローに不要なため)。ただし `measure` は結果表示用に持つ

### 8.2 変換スクリプト

`tools/` ディレクトリに MusicXML → 上記 JSON の変換スクリプト(Node.js)を用意する。アプリ本体には含めず、開発時ツールとする。

### 8.3 初期収録曲

- バイエル練習曲から 3 曲程度(片手のみの初期番号 1 曲 + 両手曲 2 曲)
- 加えて動作確認用の「ドレミファソファミレド」(右手 1〜5 指)のテスト曲を必ず入れる

---

## 9. 画面一覧

| 画面 | 内容 |
|------|------|
| S-01 セットアップ | カメラ/MIDI デバイス選択(F-01, F-02) |
| S-02 キャリブレーション | ウィザード形式(F-03) |
| S-03 楽曲選択 | 内蔵曲一覧(F-04) |
| S-04 練習 | メイン画面(F-05) |
| S-05 結果 | サマリー(F-06) |
| S-06 設定 | モーダルで可(F-07) |

- スマホ縦持ちを基本レイアウトとしつつ、PC 開発中も崩れないレスポンシブとする
- 練習画面は「画面を見なくても使える」前提なので、情報の詰め込みより視認性(遠目でも分かる大きな表示)を優先

---

## 10. 非機能要件

| 項目 | 目標 |
|------|------|
| 手認識の処理速度 | PC で 20fps 以上、Android ミドルレンジで 10fps 以上(fps が落ちても判定は Note On 駆動なので破綻しない設計とする) |
| 判定遅延 | Note On からフィードバック音まで 200ms 以内 |
| プライバシー | カメラ映像は端末外に一切送信しない(すべてローカル処理)。この旨を画面に明記 |
| ブラウザ権限 | カメラ・MIDI の権限拒否時に分かりやすいエラーと再試行導線を出す |

---

## 11. 開発フェーズ

| フェーズ | 内容 | 完了条件 |
|----------|------|----------|
| P1 | プロジェクト雛形 + バーチャル MIDI + 判定エンジン(スコアフォロー・音判定のみ) | バーチャル鍵盤でテスト曲を弾くと音の正誤が判定される。ユニットテストが通る |
| P2 | MediaPipe Hands 組み込み + カメラプレビュー + ランドマーク描画 | Web カメラで両手 21 点が安定して描画される |
| P3 | キャリブレーション + 指特定ロジック | 実カメラ + バーチャル MIDI(または実キーボード)で、押した指が推定・表示される |
| P4 | 運指判定の統合 + フィードバック音 + 練習画面完成 | テスト曲でわざと違う指で弾くとビープが鳴る |
| P5 | 結果サマリー + 設定 + バイエル曲データ整備 | 一連の練習フローが通る |
| P6 | Android 実機検証 + PWA 化 + HTTPS デプロイ | Android Chrome 単体で全機能が動く |

P1 と P2 は独立しているため並行可能。**P3(指特定)が最大の技術リスクなので、P1/P2 完了後は最優先で着手する。**

---

## 12. リスクと対策

| リスク | 対策 |
|--------|------|
| 指の重なり・オクルージョンで誤検出(特に指くぐり) | 判定不能カテゴリを設けて無理にミス判定しない。カメラ角度のガイドをセットアップ画面に表示 |
| 照明条件で認識精度が落ちる | セットアップ画面で認識状態(手が見えているか)をリアルタイム表示し、環境調整を促す |
| Android での MediaPipe 性能不足 | 解像度を下げる設定(640×360 等)を用意。判定は Note On 駆動なので低 fps でも成立する設計 |
| カメラフレームと MIDI の時刻ズレ | ランドマーク履歴のリングバッファ(7.2 参照) |
| microKEY2 Air のミニ鍵盤は実ピアノと鍵盤幅が違う | 卒制の論点として「ミニ鍵盤での検証」と明記。キャリブレーションが実鍵盤ジオメトリ上の線形補間のため鍵盤サイズには非依存 |
| Bluetooth MIDI の遅延・切断 | BLE-MIDI は USB より遅延が大きい(目安 +10〜20ms)が、タイミング評価をしない本アプリでは許容範囲。切断時はセットアップ画面へ誘導する。問題が出る場合は USB 接続に切り替え可能 |
| Web MIDI が Android Chrome で権限エラーになる | HTTPS 配信を徹底。権限フローのハンドリングを F-01 で実装 |

---

## 13. ディレクトリ構成(案)

```
src/
  core/            # 判定エンジン(UI非依存・テスト対象)
    scoreFollower.ts
    fingerEstimator.ts
    judge.ts
    types.ts
  midi/
    midiInput.ts       # Web MIDI ラッパー
    virtualKeyboard.ts # バーチャルMIDI
  vision/
    handTracker.ts     # MediaPipe ラッパー + リングバッファ
    calibration.ts
  audio/
    feedback.ts        # ビープ生成
  songs/               # 楽曲JSON
  components/          # React コンポーネント(画面単位)
  App.tsx
tools/
  musicxml2json.mjs    # 変換スクリプト
tests/
  core/                # Vitest
```

---

## 変更履歴

- 2026-09-03: **F-08 参照音の再生**を追加(開発者からの要望)。理由: MIDI キーボードが音源を持たないため、
  押した音が鳴らず練習にならない。あわせて 5 章の音の欄を「ビープ音と参照音の生成」に更新。

- 2026-07-07: F-03 手順 4 の補間方式を「半音単位で等間隔と近似」から「実鍵盤ジオメトリ(白鍵等幅・黒鍵は白鍵の境目)に基づく線形補間」に変更。あわせて 7.2 の信頼度の単位を「平均半音間隔 = オクターブ幅の 1/12」に再定義(閾値 0.6 / 0.25 / 0.5 の値は不変)。理由: P3 の実機テストで、半音等間隔近似が実鍵盤(白鍵等幅)に対して最大約 0.7 平均半音の系統誤差を持つことを確認したため。開発者承認済み。
